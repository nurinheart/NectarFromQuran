# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Temporary outputs (not archived)
output/*.png

# Archive folder is TRACKED (permanent storage in git)
# !archive/

# Session files (sensitive)
session.json
*.json.backup

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Environment
.env

# NOTE: These ARE tracked by git (needed for production):
# - posted_verses.json (tracks posted verses, prevents repeats)
# - tafsir_cache.json (reduces API calls, improves reliability)
# - quran_cache.json (reduces API calls, improves reliability)
